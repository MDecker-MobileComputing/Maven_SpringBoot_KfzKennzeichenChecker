/**
 * Dieses Paket enthält das Backend einer Anwendung, mit der deutsche KFZ-Kennzeichen
 * überprüft werden können. Dabei wird auch ein externer Services über HTTP aufgerufen,
 * der das Unterscheidungszeichen auflösen soll. 
 */
package de.eldecker.dhbw.spring.kennzeichenchecker;